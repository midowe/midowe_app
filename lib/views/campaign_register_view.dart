import 'package:flutter/material.dart';

class CampaignRegisterView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Campaign profile"),
      ),
    );
  }
}
