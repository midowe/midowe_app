import 'package:flutter/material.dart';

class CampaignList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Main", style: TextStyle(fontSize: 50),),
    );
  }

}